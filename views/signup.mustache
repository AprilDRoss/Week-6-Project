<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Sign Up</title>
  </head>
  <body>
    <div class="container">
      <!-- established an endpoint by using the "/" send it thru a post request -->
      {{#userNotFound}}
      {{.}}
      {{/userNotFound}}

      {{#errors}}
      {{.}}
      {{/errors}}

        <nav class="otherpages">
          <a href="homepage.mustache">Gabble</a>
          <a href ="creategabble.mustache">Create a new gab</a>
          <a href="login.mustache">Login</a>
          <a href="#">Sign Up</a>
        </nav>

        <h1 class="signupHeader">Sign Up</h1>

      <form action="/signup" method="post">
        <label for="username">Username:</label>
        <input type="text" name="username" value="" placeholder=" "><br>

        <label for="password">Password:</label>
        <input type="password" name="password" value="" placeholder=" "><br>
        <br>
        <br>
        <label for="password">Confirm Password:</label>
        <input type="password" name="confirmpassword" value="" placeholder=" "><br>
        <button type="submit">Sign Up</button>
      </form>
  </div>
  </body>
</html>
